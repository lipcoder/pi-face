[Unit]
Description=Face Docker Compose Service
After=network-online.target docker.service
Wants=network-online.target docker.service

[Service]
Type=oneshot
User=server
WorkingDirectory=/home/server/project/face

# 注意：如果你系统是 docker-compose，而不是 `docker compose`，
# 把下面两行里的 `docker compose` 换成 `docker-compose`
ExecStart=/usr/bin/docker compose up -d
ExecStop=/usr/bin/docker compose down

RemainAfterExit=yes

[Install]
WantedBy=multi-user.target
